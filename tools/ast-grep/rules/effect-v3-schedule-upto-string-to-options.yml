id: effect-v3-schedule-upto-string-to-options
language: TypeScript
message: Schedule.upTo takes an options object in v4, not a bare duration.
severity: warning
rule:
  pattern: Schedule.upTo($DURATION)
constraints:
  DURATION:
    not:
      kind: object
fix: "Schedule.upTo({ duration: $DURATION })"
